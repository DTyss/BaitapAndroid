<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.danh.thlab04">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.THLab04">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="THLab04 - Menu chính">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BÀI TẬP 1: Login + Custom Toast -->
        <activity
            android:name=".Baitap1Activity"
            android:exported="false"
            android:label="Bài 1 - Login Custom Toast"
            android:parentActivityName=".MainActivity" />

        <!-- BÀI TẬP 2: Từ vựng + ListView chi tiết -->
        <activity
            android:name=".Baitap2Activity"
            android:exported="false"
            android:label="Bài 2 - Từ vựng"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".DetailVocabActivity"
            android:exported="false"
            android:label="Chi tiết từ vựng"
            android:parentActivityName=".Baitap2Activity" />

        <!-- BÀI TẬP 3: Emoji + Random All -->
        <activity
            android:name=".Baitap3Activity"
            android:exported="false"
            android:label="Bài 3 - Emoji Random"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>